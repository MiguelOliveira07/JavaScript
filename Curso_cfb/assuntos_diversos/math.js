const div_num = document.querySelector("#num")

// div_num.innerHTML = Math.sqrt(16)
// div_num.innerHTML = Math.PI
// div_num.innerHTML = Math.random()*10
// const meu_num = Math.random()*10
// div_num.innerHTML = Math.floor( meu_num)+1 //evita gerar o 0
// div_num.innerHTML = Math.round( meu_num)
div_num.innerHTML = Math.pow(2,4)
